# Default development mode. Keeps the app runnable without cloud credentials.
GEMMA_GARDEN_MODE=mock

# Future Google Cloud / Vertex AI configuration.
# Do not commit real credentials or service account JSON files.
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
GEMMA_MODEL_ID=gemma-4-26b-a4b-it-maas
